/*
 * LogoutModal Messages
 *
 * This contains all the text for the LogoutModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LogoutModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LogoutModal component!',
  },
});
