// import { selectImportedIntentListDomain } from '../selectors';

describe('selectImportedIntentListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
