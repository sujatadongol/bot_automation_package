// import { selectIntentImportPageDomain } from '../selectors';

describe('selectIntentImportPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
