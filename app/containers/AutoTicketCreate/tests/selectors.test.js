// import { selectTicketSettingDomain } from '../selectors';

describe('selectTicketSettingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
