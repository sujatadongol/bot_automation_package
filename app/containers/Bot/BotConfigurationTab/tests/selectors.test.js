// import { selectBotConfigurationTabDomain } from '../selectors';

describe('selectBotConfigurationTabDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
