// import { selectCreateIntentPageDomain } from '../selectors';

describe('selectCreateIntentPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
