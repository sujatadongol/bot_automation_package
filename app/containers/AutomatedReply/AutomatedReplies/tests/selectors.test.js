// import { selectAutomatedRepliesDomain } from '../selectors';

describe('selectAutomatedRepliesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
