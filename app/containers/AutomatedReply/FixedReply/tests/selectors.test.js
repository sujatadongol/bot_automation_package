// import { selectCreateFixedReplyDomain } from '../selectors';

describe('selectCreateFixedReplyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
