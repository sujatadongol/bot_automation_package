// import { selectCreateEntityModalContainerDomain } from '../selectors';

describe('selectCreateEntityModalContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
